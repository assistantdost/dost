export const metadata = {
	title: "MCP Servers Explorer",
	description: "Explore the available Model Context Protocol (MCP) servers and tools in the DOST ecosystem.",
};

export default function McpServersLayout({ children }) {
	return children;
}
